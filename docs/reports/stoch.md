# stoch: converted

plate GLSL lines: 52   positive lines: 110

gaps: none reported

notes: The converting agent was terminated by a model usage limit after
writing this positive and passing its self-checks, but before writing
its own report. This report is the session lead's, and it records only
what the lead verified directly, which is less than the other agents
recorded for their plates.

Verified here: emit produces GLSL with no refusal; smoke passes all
five rows (defaults, defaults at t=1.7, two hashed lever settings);
and the browser pair check against the original plate at identical
levers gives r=see sweep-wave2.log beside this file (correlation, total ratio, point budget).

NOT verified here: the point-for-point cross-check against a literal
transcription of the plate's GLSL that the other agents ran on their
own conversions. The picture agrees; the per-point identity is
untested. Worth doing if this plate is ever promoted past decoration.
