# e8: converted

plate GLSL lines: 154   positive lines: 266

gaps: none. The agent was warned that a runtime-indexed constant table
would be a blocker; the E8 roots did not need one.

notes: The converting agent was terminated by a model usage limit
immediately after finishing this file, so this report is the session
lead's. Verified here: emit produces 266 GLSL lines with no refusal;
smoke passes all five rows (18 declines per 20000 at every setting,
consistent across levers and clock); the browser pair check against
the original at identical levers gives r=0.9981, ratio 0.998.

NOT verified here: the point-for-point cross-check against a literal
transcription of the plate's GLSL. The picture agrees; the per-point
identity is untested.
