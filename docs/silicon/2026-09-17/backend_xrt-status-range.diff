diff --git a/host/src/backend_xrt.cpp b/host/src/backend_xrt.cpp
index 023a808..882e5b8 100644
--- a/host/src/backend_xrt.cpp
+++ b/host/src/backend_xrt.cpp
@@ -279,6 +279,7 @@ constexpr uint32_t MODE_SEQ = 1u << 15;
 constexpr uint32_t ST_BUS_BITS  = 0x7u;
 constexpr uint32_t ST_REFUSED   = 0x8u;
 constexpr uint32_t ST_DEPOSIT_OVERFLOW = 0x10u;
+constexpr uint32_t ST_SCRATCH_RANGE    = 0x20u;   /* CFT_STATUS_SCRATCH_RANGE, R8 */
 
 /* Round `n` up to a whole 256-bit beat's worth of bytes. The masters
  * move whole beats whatever the format, so a buffer that ends mid-beat
@@ -2023,7 +2024,7 @@ extern "C" int cftx_program_run(void *hw, int fmt, const void *image,
             st_acc = 0;
         }
         if (bus)
-            *bus = st_acc & ST_DEPOSIT_OVERFLOW;
+            *bus = st_acc & (ST_DEPOSIT_OVERFLOW | ST_SCRATCH_RANGE);
         set_err(err + " - the compute unit may still be active, so this "
                       "handle is finished; close and reopen it" +
                 (st_acc ? " (STATUS 0x" + hex32(st_acc) + ")"
@@ -2183,7 +2184,7 @@ extern "C" int cftx_program_run(void *hw, int fmt, const void *image,
      * is the tail. Telling it the results are invalid would be a
      * worse lie than saying nothing. */
     if (bus)
-        *bus = status_acc & ST_DEPOSIT_OVERFLOW;
+        *bus = status_acc & (ST_DEPOSIT_OVERFLOW | ST_SCRATCH_RANGE);
     return ST_OK;
 }
 
